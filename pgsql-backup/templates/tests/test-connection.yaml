apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pgsql-backup.fullname" . }}-test-connection"
  labels:
    {{- include "pgsql-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pgsql-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
